# Agents

This directory is intentionally model-agnostic.

- `../AGENTS.md` is the canonical instruction file.
- `skills/` contains reusable Markdown skill playbooks.
- `config.example.json` is a neutral example for tools that can import custom agent configuration.

If a specific agent requires its own config format, create a local ignored adapter and keep it out of public commits.

